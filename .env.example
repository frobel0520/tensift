# Optional local API origin. Leave empty for same-origin Pages Functions.
VITE_API_BASE_URL=

# Optional Google AdSense setup. These are public identifiers, not secrets.
# Ads stay disabled until both values are valid and the site is approved by Google.
VITE_ADSENSE_CLIENT_ID=
VITE_ADSENSE_TOP_SLOT=
